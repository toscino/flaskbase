# Default install (repo root, venv active):
#   pip install -r example/requirements.txt
# Same pinned wheel for local runs and App Engine deploy. Bump URL when you tag a new release.
#
# Library contributors only: pip install -r requirements-dev.txt  (editable -e flask_base)

flask-base @ https://github.com/toscino/flaskbase/releases/download/v0.3.0/flask_base-0.3.0-py3-none-any.whl
gunicorn>=21.0.0
