# Local development: install from repo root (not this folder):
#   cd ..
#   pip install -r requirements-dev.txt
#
# That installs flask-base in editable mode plus gunicorn.

gunicorn>=21.0.0

# Production (App Engine) — use Artifact Registry instead of -e:
# --extra-index-url https://REGION-python.pkg.dev/PROJECT_ID/flask-base-python/simple/
# flask-base==0.2.0
