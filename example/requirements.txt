# Local development: install from repo root (not this folder):
#   cd ..
#   pip install -r requirements-dev.txt
#
# Production (App Engine): pinned GitHub Release wheel (bump URL when you tag a new version).

flask-base @ https://github.com/toscino/flaskbase/releases/download/v0.2.0/flask_base-0.2.0-py3-none-any.whl
gunicorn>=21.0.0
