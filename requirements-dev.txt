# Library contributors only — live-edit flask_base/ in this monorepo.
# Clone users and normal local runs: pip install -r example/requirements.txt
#   (same pinned GitHub Release wheel as App Engine deploy)

-e flask_base
pytest>=7.0.0
gunicorn>=21.0.0
